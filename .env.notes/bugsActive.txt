BUG: webpack ... "webpack <5 used to include polyfills for node.js core modules by default. This is no longer the case.  Verify if you need this module and configure a polyfill for it."
DETAILS:
    - this has popped up a couple of times
    - after making some changes to .env, some things broke
    - problem re-emerged after editing client/src/config.js
    - using previous code in config.js avoids error, but does not hit the right .env folder, and data does not get saved to db from frontend
PARTIAL FIX:
    -  npm installing the package in the file location fixed some errors
    - add code to offending packages package.json: 
            "browser": {
            "fs": false,
            "path": false,
            "os": false
        },
    - possible solution: https://stackoverflow.com/questions/57161839/module-not-found-error-cant-resolve-fs-in
FIX 1:
    - Sartaj helped me fix this initially...
    - 2nd time I fixed by editing server/index.js env variables
    - using previous code avoids error, but does not hit the right .env folder
FIX FOR REACT:
    - cannot use 'dotenv' package in react!
    - env variable names MUST start with "REACT_APP_"
    - use this syntax:
    const API_URL = process.env.REACT_APP_SERVER_API_URL;

GENERAL ENV SYNTAX USING DOTENV:
import environment variables + error if fail
const env = require("dotenv").config({ path: ".env" });
const envMongoUser = env.parsed.MONGO_USER;
const envMongoPass = env.parsed.MONGO_PASS;
const envStaticDir = env.parsed.STATIC_DIR;
const envDomain = env.parsed.DOMAIN;
const envStripeSecretKey = env.parsed.STRIPE_SECRET_KEY



