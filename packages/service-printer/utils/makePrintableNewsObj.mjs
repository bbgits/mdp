
import fetch, { Headers } from 'node-fetch';
import { MongoClient } from 'mongodb'; // IMORT MONGO

export default async function makePrintableNewsObj(myType, myData1, myData2, myData3, myData4, myData5) {

    var testReturnMsg = await ("Making Printable News Obj ... ")

    return await testReturnMsg;

}